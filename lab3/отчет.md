 Отчет. Линейная модель и градиентный спуск
 
 Я открыл jupyter notebook с линейной моделью и дописал недостающий код для классов линейных моделей. Я реализовал вычисление предсказаний, функцию потерь и градиенты. Я проверял, что код запускается без ошибок.
 
 Я загрузил данные из файлов german.csv и Student_Performance.csv в ноутбук. Для этого я использовал функции чтения таблиц из pandas.
 
 Я предобработал данные. Я посмотрел на столбцы и типы данных, убрал лишние или проблемные признаки, обработал пропуски, закодировал категориальные признаки и при необходимости нормализовал числовые признаки.
 
 Я выбрал параметры обучения для градиентного спуска. Я перебирал разные значения скорости обучения и количества итераций, смотрел, как меняется функция потерь и качество модели, и подобрал такие параметры, при которых градиентный спуск сходился и модель обучалась устойчиво.
 
 Я обучил линейные модели на подготовленных данных с помощью написанного градиентного спуска. Я запустил обучение и проверил, что значение функции потерь уменьшается с итерациями.
 
 Я проверил точность классификатора. Я разделил данные на обучающую и тестовую выборки, посчитал качество модели на тестовой выборке (например, долю правильных ответов) и сделал выводы о том, насколько хорошо модель классифицирует объекты.
 
 Я открыл второй ноутбук с моделями деревьев и KNN. Я поэкспериментировал с различными параметрами этих моделей (глубина дерева, количество соседей и другие настройки), обучил модели на тех же данных и сравнил их качество с линейной моделью. Я не использовал ансамбли, как указано в задании.
 
 По результатам экспериментов я сделал простые выводы о том, какие модели лучше подошли для данных, как влияли параметры обучения и предобработки, и какие сложности возникали при выполнении лабораторной работы.
 

 
